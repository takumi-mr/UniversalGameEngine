{
  // 厳しさは ../../tsconfig.base.json に統一。
  // フロント（ブラウザ）とバックエンド（Bun）の両方から使われるので lib に DOM を含める。
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "types": ["bun"]
  },
  "include": ["."]
}
